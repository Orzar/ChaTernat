<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="internat.chaternat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="internat.acitivity.chaternat.LobyAcitivty"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="internat.acitivity.chaternat.QuestionActivity"
            android:label="@string/title_activity_question" >
        </activity>
        <activity
            android:name="internat.acitivity.chaternat.SerieActivity"
            android:label="@string/title_activity_serie"
            android:parentActivityName="internat.acitivity.chaternat.LobyAcitivty" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="internat.acitivity.chaternat.LobyAcitivty" />
        </activity>
    </application>

</manifest>
